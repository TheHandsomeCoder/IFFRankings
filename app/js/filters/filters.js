angular.module('RankingsApp')
.filter('matchAccessLevel', function() 
{
	return function( fencers, results) 
	{
		var _fencers = _.reject(fencers, function(fencer){ return  });
		return filtered;
	};
});